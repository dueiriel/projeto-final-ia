# PDF.js

O servidor Express expõe os arquivos de `pdfjs-dist/build` em `/vendor/pdfjs`.

Depois de executar `npm install`, o navegador carrega:

- `/vendor/pdfjs/pdf.mjs`
- `/vendor/pdfjs/pdf.worker.mjs`

